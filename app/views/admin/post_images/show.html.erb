<h1>show</h1>

<div>
  <%= image_tag @post_image.get_image, :size => '200x200'  %>
  <p>タイトル：<%= @post_image.title %></p>
  <p>説明：<%= @post_image.caption %></p>
  <p>投稿ユーザーペット画像：<%= image_tag @post_image.customer.get_profile_image(100,100)%></p>
  <p>ユーザーネーム：<%= @post_image.customer.pen_name %></p>
  <p>投稿日：<%= @post_image.created_at.strftime('%Y/%m/%d') %></p>
    <%= link_to "削除", admin_post_image_path(@post_image), method: :delete %>
</div>
<div>
  <td class="view-count">閲覧数: <%= @post_image.view_counts.count %></td>
  <p>コメント件数：<%= @post_image.post_comments.count %></p>
  <% @post_image.post_comments.each do |post_comment| %>
    <p><%= image_tag post_comment.customer.get_profile_image(100,100) %></p>
    <%= post_comment.customer.name %>
    <%= post_comment.created_at.strftime('%Y/%m/%d') %><%= post_comment.comment %>
      <%= link_to "削除", admin_post_image_post_comment_path(post_comment.post_image, post_comment), method: :delete %>
    <% end %>

</div>
