<% if customer_signed_in? %>
  <%= form_with url: search_path, method: :get, local: true do |f| %>
    <%= f.text_field :content %>
    <%= f.select :model, options_for_select({ "会員" => "customer", "投稿" => "post_image" }) %>
    <%= f.submit '検索' %>
  <% end %>
<% end %>